/*¨The table is created here because when it starts with the room it doesn't get enough time to
get all the variables from other objects (because they are not set yet) */
instance_create_depth(x,y,-1000,obj_table);